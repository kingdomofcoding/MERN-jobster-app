services:
  - name: my-node-server
    type: web
    env: node
    rootDir: server # ✅ Set 'server/' as the working directory
    buildCommand: "npm install && npm run build --prefix ../client"
    startCommand: "node server.js"
